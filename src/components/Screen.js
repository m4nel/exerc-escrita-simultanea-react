import { useState } from "react"

const Screen = (e, props) = => {
const [nome, sobrenome, idade, Setname] = useState("")

const handleInput = (event) = {
    Setname(event.target.value);
}

    return (
        

    )
}


export default Screen